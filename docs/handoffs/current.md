# Current handoff

No interrupted work is active.

The repository is synchronized on `main`.

## Current priority

Complete the ForecastOps proof of concept using the existing atmospheric, wave,
sea-surface-temperature, and tide pipeline.

Next sequence:

1. run the complete pipeline live for all five configured locations
2. fix only blocking or data-correctness defects
3. build one integrated coastal-conditions dataset
4. expose one simple usable output

Refer to `docs/roadmap.md` and `docs/scope-register.md` only when selecting or
changing proof-of-concept scope.

Do not update this handoff for routine completed work. Update it only when work
is interrupted, blocked, or cannot be reconstructed cheaply from Git and
GitHub.